# Server port (default: 3001)
PORT=3001

# Google reCAPTCHA v3 (optional)
# Set to enable spam protection - must match frontend site key
# Get keys from: https://www.google.com/recaptcha/admin
# RECAPTCHA_SECRET_KEY=your-secret-key

# Minimum reCAPTCHA score to accept (0.0 to 1.0, default: 0.5)
# Higher = stricter (1.0 = likely human, 0.0 = likely bot)
# RECAPTCHA_MIN_SCORE=0.5
